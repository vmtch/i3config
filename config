# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mod2 Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font pango:uzura_font 0

# window gap
gaps inner 10
gaps outer 5

# Border Width
default_border pixel 0
default_floating_border pixel 0

# Titlebar
for_window [class=".*"] title_format "<span alpha='1'>%title</span>"

# Color Settings
# class                 border  backgr. text    indicator child_border
client.focused          #ffb6c1 #ffb6c1 #000000 #2e9ef4   #ffb6c1
client.focused_inactive #ffe4e1 #ffe4e1 #696969 #484e50   #ffe4e1
client.unfocused        #ffe4e1 #ffe4e1 #888888 #484e50   #ffe4e1
client.urgent           #2f343a #ffe4e1 #696969 #484e50   #ffe4e1
client.placeholder      #ffe4e1 #ffe4e1 #696969 #484e50   #ffe4e1

client.background       #FF3333


# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
# exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
#exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

workspace 1 output VGA-1-1
workspace 2 output DVI-D-1-1
workspace 3 output HDMI1
workspace 4 output DVI-D-1-2
workspace 5 output HDMI2

# start a terminal
bindsym $mod+Return exec --no-startup-id sh -c 'alacritty & sleep 0.1 && ~/.config/i3/mouse_follow_focus.sh'
bindsym $mod2+Return exec --no-startup-id sh -c 'alacritty & sleep 0.1 && ~/.config/i3/mouse_follow_focus.sh'


# kill focused window
bindsym $mod+q kill; exec --no-startup-id sh -c 'sleep 0.05 && ~/.config/i3/mouse_follow_focus.sh'
bindsym $mod2+q kill; exec --no-startup-id sh -c 'sleep 0.05 && ~/.config/i3/mouse_follow_focus.sh'

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id ~/.config/i3/run_and_focus.sh
bindsym $mod2+d exec --no-startup-id ~/.config/i3/run_and_focus.sh

# change focus
bindsym $mod+h focus left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod+j focus down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod+k focus up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod+l focus right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+h focus left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+j focus down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+k focus up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+l focus right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh

# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod+Shift+j move down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod+Shift+k move up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod+Shift+l move right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+Shift+h move left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+Shift+j move down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+Shift+k move up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
bindsym $mod2+Shift+l move right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh

# move focused workspace
bindsym $mod+Ctrl+h  swap container to 
bindsym $mod+Ctrl+j  swap container to 
bindsym $mod+Ctrl+k  swap container to 
bindsym $mod+Ctrl+l  swap container to 
bindsym $mod2+Ctrl+h swap container to 
bindsym $mod2+Ctrl+j swap container to 
bindsym $mod2+Ctrl+k swap container to 
bindsym $mod2+Ctrl+l swap container to 

# alternatively, you can use the cursor keys:
#bindsym $mod+Shift+Left move left
#bindsym $mod+Shift+Down move down
#bindsym $mod+Shift+Up move up
#bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+shift+' split h

# split in vertical orientation
bindsym $mod+Down split v
#bindsym $mod+shift+5 split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

bindsym --release $mod+space exec --no-startup-id xdotool click 1
bindsym $mod+shift+w exec --no-startup-id xdotool mousemove_relative -- 0 -20
bindsym $mod+shift+a exec --no-startup-id xdotool mousemove_relative -- -20 0
bindsym $mod+shift+s exec --no-startup-id xdotool mousemove_relative -- 0 20
bindsym $mod+shift+d exec --no-startup-id xdotool mousemove_relative -- 20 0

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1; [con_id="__focused__"] focus; workspace number $ws1;
bindsym $mod+Shift+2 move container to workspace number $ws2; [con_id="__focused__"] focus; workspace number $ws2;
bindsym $mod+Shift+3 move container to workspace number $ws3; [con_id="__focused__"] focus; workspace number $ws3;
bindsym $mod+Shift+4 move container to workspace number $ws4; [con_id="__focused__"] focus; workspace number $ws4;
bindsym $mod+Shift+5 move container to workspace number $ws5; [con_id="__focused__"] focus; workspace number $ws5;

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        #bindsym Left resize shrink width 10 px or 10 ppt
        #bindsym Down resize grow height 10 px or 10 ppt
        #bindsym Up resize shrink height 10 px or 10 ppt
        #bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindcode Control+34 mode "default"
}

bindsym $mod+r mode "resize"
bindsym $mod2+r mode "resize"


mode "move window" {
    bindsym h move left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym j move down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym k move up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym l move right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod+h focus left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod+j focus down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod+k focus up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod+l focus right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod2+h focus left; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod2+j focus down; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod2+k focus up; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh
    bindsym $mod2+l focus right; exec --no-startup-id ~/.config/i3/mouse_follow_focus.sh

    bindsym Return mode "default"; gaps inner all set 15
    bindsym Escape mode "default"; gaps inner all set 15
    bindcode Control+34 mode "default"; gaps inner all set 15
}

bindsym $mod+a mode "move window"; gaps inner all set 30
bindsym $mod2+a mode "move window"; gaps inner all set 30

# Sleep
bindsym $mod+Shift+BackSpace exec ~/.config/i3/lock.sh


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#    font pango:uzura_font 12
#        status_command echo date
#        colors {
#            background #ffb6c1
#            statusline #000000
#            separator #ffb6c1
#
#            focused_workspace   #ffb6c1 #ffb6c1 #000000 #2e9ef4   #ffb6c1
#            active_workspace    #ffe4e1 #ffe4e1 #696969 #484e50   #ffe4e1
#            inactive_workspace  #333333 #ffe4e1 #888888 #484e50   #ffe4e1
#            urgent_workspace    #2f343a #ffe4e1 #696969 #484e50   #ffe4e1
#        }
#}
exec_always --no-startup-id ~/.config/polybar/launch.sh

# Background
#exec_always feh -s --randomize --slideshow-delay 1 --output-ouly HDMI1 --bg-scale ~/Pictures/wallpaper/ &
exec_always --no-startup-id ~/.config/i3/slideshow.sh

# Window Compositor
exec_always --no-startup-id picom -f

# Keyring Settings
#exec --no-startup-id /usr/bin/gnome-keyring-daemon --start --components=secrets

# Key Repeat Settings
exec --no-startup-id xset r rate 220 30

# fcitx5
exec_always --no-startup-id fcitx5 -d

# Auto Suspend Settings
exec_always --no-startup-id xset -dpms
exec_always --no-startup-id xset s off
exec_always --no-startup-id xset s noblank
